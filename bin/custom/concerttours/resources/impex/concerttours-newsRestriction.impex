INSERT_UPDATE SearchRestriction; code[unique = true]; name[lang = en]; query                                                                                                                                                                               ; principal(UID); restrictedType(code); active; generate
                               ; NewsOnlyOnline     ; NewsOnline     ; {catalogVersion} IN ({{ SELECT {cv.pk} FROM {CatalogVersion AS cv JOIN Catalog AS c ON {cv.catalog}={c.pk}} WHERE {c.id}='concertToursContentCatalog' AND {cv.version}='Online' }}) ; customergroup ; News                ; true  ; true
